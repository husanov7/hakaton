import React$1 from 'react';

interface HeartProps {
  width: number
  height: number
  active: boolean
  onClick: () => void
  activeColor?: string
  inactiveColor?: string
  strokeWidth?: number
  disabled?: boolean
  ariaLabel?: string
  className?: string
  style?: React.CSSProperties
}

declare function Heart({ width, height, active, onClick, activeColor, inactiveColor, strokeWidth, disabled, ariaLabel, className, style }: HeartProps): React$1.JSX.Element;

export { Heart as default };
